# Complete Modern Social Network App

- using `Django`, `Docker`, `uv`, `postgres`, `nginx`, `gunicorn`, `Bootstrap`, `Sass`, `HTMX` & so on.
- used many professional methods. like modularity, reusability, security & so on.
- used `uv`: for dependency management, `django-environ`: store environments & so on.
